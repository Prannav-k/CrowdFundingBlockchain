{
  "name": "test-bank",
  "version": "0.0.1",
  "description": "Basic bank app",
  "scripts": {
    "prepublish": "mkdirp ./dist && composer archive create --sourceType dir --sourceName . -a ./dist/test-bank.bna",
    "pretest": "npm run lint",
    "lint": "eslint .",
    "test": "nyc mocha -t 0 test/*.js && cucumber-js"
  },
  "author": "Prannavk",
  "email": "s",
  "license": "s",
  "devDependencies": {
    "composer-admin": "^0.18.2",
    "composer-cli": "^0.18.2",
    "composer-client": "^0.18.2",
    "composer-common": "^0.18.2",
    "composer-connector-embedded": "^0.18.2",
    "composer-cucumber-steps": "^0.18.2",
    "chai": "latest",
    "chai-as-promised": "latest",
    "cucumber": "^2.2.0",
    "eslint": "latest",
    "nyc": "latest",
    "mkdirp": "latest",
    "mocha": "latest"
  }
}
